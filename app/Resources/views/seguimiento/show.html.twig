{% extends 'templates/layout.html.twig' %}
{% block body %}

<div class="text-center container col-md-8 mt-5 border-violet card">
  <h1 class="mt-2 mb-2"><strong>Seguimiento</strong></h1>
  <hr>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Fecha</th>
        <th>Descripción</th>
      </tr>
    </thead>
    <tbody>
        <tr>
            <td>{% if seguimiento.fecha %}{{ seguimiento.fecha|date('Y-m-d') }}{% endif %}</td>
            <td>{{ seguimiento.texto }}</td>
        </tr>
    </tbody>
  </table>

  <hr>
  <div class="row justify-content-center">
    <div class="col-md-3 col-sm-4 col-4">
      <a class="btn btn-volver" href="/seguimiento/index/{{seguimiento.expediente.id}}" ><i class="material-icons align-bottom">keyboard_backspace</i>Volver</a>
    </div>
    <div class="col-md-3 col-sm-4 col-4">
      <a class="btn btn-violet" href="{{ path('seguimiento_edit', { 'id': seguimiento.id }) }}">Edit</a>
    </div>
  </div>
</div>

{% endblock %}
