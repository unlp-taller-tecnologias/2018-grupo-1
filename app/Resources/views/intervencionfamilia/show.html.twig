{% extends 'base.html.twig' %}

{% block body %}
    <h1>Intervencionfamilium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intervencionFamilium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ intervencionFamilium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('intervencionfamilia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('intervencionfamilia_edit', { 'id': intervencionFamilium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
