{% extends 'templates/layout.html.twig' %}

{# app/Resources/views/layout.html.twig #}
{% block head %}
	<title>Listado de {{ parametro }}</title>
{% endblock %}

{% block body %}
<div class="text-center container col-md-6 mt-3">
	<div>
		<a href="/alta/{{ entidad }}" class="btn btn-violet">Cargar nuevo elemento en {{ parametro }}</a>
	</div>
	<table class="table table-striped table-bordered">
		<thead>
			<th>Descipcion</th>
			<th>Modificar</th>
			<th>Eliminar</th>
		</thead>
		<tbody>
			{% for elemento in elementos %}
				<tr>
					<td>{{ elemento.nombre }}</td>
					<td><a href="/update/{{ entidad }}/{{ elemento.id }}">Modificar</a></td>
					<td><a href="/delete/{{ entidad }}/{{ elemento.id }}">Eliminar</a></td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
</div>
{% endblock %}
