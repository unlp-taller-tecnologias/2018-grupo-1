{% extends 'templates/layout.html.twig' %}
{% block head %}
  <title>{% if alta == 1 %}Alta de{% else %}Modificar{% endif %} {{ entidad }}</title>
{% endblock %}
{% block body %}
  <div class="text-center container col-md-6 mt-5">
    <h1>{% if alta == 1 %}Alta de{% else %}Modificar{% endif %} {{ entidad }}</h1>
    {{ form_start(form) }}
    {#{{ form_widget(form) }}#}
    {{ form_row(form.descripcion, {'attr': {'class': 'form-control1', 'placeholder':'Descripcion', 'autofocus':true} })}}
	{% if form.orden is defined %}{{ form_row(form.orden, {'attr': {'class': 'form-control1', 'placeholder':'Numero de orden', 'autofocus':true} })}}{% endif %}
    {{ form_end(form) }}
  </div>
{% endblock %}
