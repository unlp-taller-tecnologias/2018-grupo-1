{% extends 'templates/layout.html.twig' %}

{% block body %}
    <h1>Botonantipanico</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ perimetral.id }}</td>
            </tr>
            <tr>
                <th>Fechaentrega</th>
                <td>{% if perimetral.fecha %}{{ perimetral.fecha|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('botonantipanico_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('botonantipanico_edit', { 'id': perimetral.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
