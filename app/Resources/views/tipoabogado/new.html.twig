{% extends 'templates/layout.html.twig' %}

{% block body %}
    <div class="text-left container col-md-6 mt-3">
        <h3>Alta Tipo de abogadx</h3>
        <div class="panel">
            <div class="panel-body"> 
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input class="btn btn-violet btn-xs" type="submit" value="Crear" />
                {{ form_end(form) }}
                <a class="btn btn-info btn-xs" href="{{ path('tipoabogado_index') }}">Volver</a>
            </div>
        </div>
    </div> 
{% endblock %}
