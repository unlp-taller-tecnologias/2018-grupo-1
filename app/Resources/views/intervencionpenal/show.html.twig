{% extends 'base.html.twig' %}

{% block body %}
    <h1>Intervencionpenal</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intervencionPenal.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ intervencionPenal.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('intervencionpenal_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('intervencionpenal_edit', { 'id': intervencionPenal.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
